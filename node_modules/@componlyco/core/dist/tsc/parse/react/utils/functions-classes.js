"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getFunctionsAndClasses = exports.filterReactClassComponent = exports.filterReactFunctionalComponent = void 0;
var ts_morph_1 = require("ts-morph");
var import_declarations_1 = require("./import-declarations");
var project_1 = require("./project");
function filterReactFunctionalComponent(node) {
    var isComponent = false;
    var sourceFile = node.getSourceFile();
    if ((0, import_declarations_1.memoizedIsStencilImportPresent)(sourceFile, "h") === false &&
        (node instanceof ts_morph_1.FunctionDeclaration || node instanceof ts_morph_1.ArrowFunction)) {
        var jsxElements = node.getDescendantsOfKind(ts_morph_1.SyntaxKind.JsxElement);
        var jsxSelfClosingElements = node.getDescendantsOfKind(ts_morph_1.SyntaxKind.JsxSelfClosingElement);
        var jsxFragments = node.getDescendantsOfKind(ts_morph_1.SyntaxKind.JsxFragment);
        isComponent = jsxElements.length > 0 || jsxSelfClosingElements.length > 0 || jsxFragments.length > 0;
    }
    return isComponent;
}
exports.filterReactFunctionalComponent = filterReactFunctionalComponent;
function filterReactClassComponent(node) {
    var isComponent = false;
    if (node instanceof ts_morph_1.ClassDeclaration) {
        var parent_1 = node.getExtends();
        if (parent_1 !== undefined) {
            isComponent = ["React.Component", "React.PureComponent"].includes(parent_1.getFullText().trim());
        }
    }
    return isComponent;
}
exports.filterReactClassComponent = filterReactClassComponent;
function getFunctionsAndClasses(project) {
    var sourceFiles = project.getSourceFiles();
    var declarationList = sourceFiles
        .filter(function (ele) {
        return (0, project_1.isStory)(ele) === false;
    })
        .flatMap(function (sourceFile) {
        var functions = sourceFile.getFunctions();
        var classes = sourceFile.getClasses();
        var arrowFunctions = sourceFile
            .getDescendantsOfKind(ts_morph_1.SyntaxKind.VariableDeclaration)
            .flatMap(function (node) { return node.getChildrenOfKind(ts_morph_1.SyntaxKind.ArrowFunction); });
        var declaration = __spreadArray(__spreadArray(__spreadArray([], __read(functions), false), __read(classes), false), __read(arrowFunctions), false);
        return declaration;
    });
    return declarationList;
}
exports.getFunctionsAndClasses = getFunctionsAndClasses;
