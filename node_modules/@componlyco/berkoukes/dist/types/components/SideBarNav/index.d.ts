/// <reference types="react" />
import type { ButtonProps } from "../Button";
import type { UserProfileProps } from "../UserProfile";
import type { GroupProps } from "@mantine/core";
export interface MenuItemsProps extends ButtonProps {
    current?: boolean;
    selector?: {
        [key: `data-${string}`]: string | number;
    };
}
export interface SideBarNavProps {
    title?: string;
    menuItems: MenuItemsProps[];
    sx?: GroupProps["sx"];
    collapse?: boolean;
    userProfile: UserProfileProps;
    selector?: {
        [key: `data-${string}`]: string | number;
    };
}
export declare function SideBarNav({ title, collapse, menuItems, sx, userProfile, selector }: SideBarNavProps): JSX.Element;
//# sourceMappingURL=index.d.ts.map