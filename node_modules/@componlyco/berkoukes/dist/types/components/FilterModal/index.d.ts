/// <reference types="react" />
import { ModalProps } from "@mantine/core";
interface DataEntry {
    value: string;
    label: string;
}
interface Section {
    data: DataEntry[];
    title: string;
    type?: 'button' | 'checkbox' | null;
}
interface FilterModalProps {
    sections: Section[];
    onValidate: (selectedFilters: Record<string, string[]>) => void;
    isOpened: boolean;
    onClose?: () => void;
    title?: string;
    sx?: ModalProps["sx"];
    size?: ModalProps["size"];
    styles?: ModalProps["styles"];
    selector?: {
        [key: `data-${string}`]: string | number;
    };
}
export declare function FilterModal({ sections, onValidate, isOpened, onClose, title, styles, sx, size, selector, }: FilterModalProps): JSX.Element;
export {};
//# sourceMappingURL=index.d.ts.map