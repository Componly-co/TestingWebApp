import type { TooltipProps as MantineTooltipProps } from "@mantine/core";
import { MouseEventHandler } from "react";
export interface TooltipPropsDefault {
    title?: string;
    description: string;
    buttonLabel?: string;
    withButton?: boolean;
    isControlled?: boolean;
    onClickButton?: MouseEventHandler<HTMLButtonElement>;
}
export interface TooltipProps extends MantineTooltipProps {
    title?: string;
    description: string;
    buttonLabel?: string;
    withButton?: boolean;
    isControlled?: boolean;
    onClickButton?: MouseEventHandler<HTMLButtonElement>;
    selector?: {
        [key: `data-${string}`]: string | number;
    };
}
export declare const Tooltip: ({ title, description, children, position, withArrow, buttonLabel, label, onClickButton, withButton, isControlled, styles, selector, ...rest }: TooltipProps) => JSX.Element;
//# sourceMappingURL=index.d.ts.map