import { jsx as _jsx } from "react/jsx-runtime";
import { Avatar as MantineAvatar } from "@mantine/core";
import { Avatar } from "../Avatar";
export function AvatarGroup(_a) {
    var _b = _a.avatars, avatars = _b === void 0 ? [] : _b, _c = _a.size, size = _c === void 0 ? "l" : _c, _d = _a.maxCount, maxCount = _d === void 0 ? 5 : _d;
    var data = avatars.slice(0, maxCount - 1);
    var remaining = maxCount - data.length;
    if (remaining > 0) {
        data.push({
            type: "text",
            letter: "+".concat(remaining),
        });
    }
    return (_jsx(MantineAvatar.Group, { children: data.map(function (_a) {
            var type = _a.type, letter = _a.letter, shape = _a.shape, src = _a.src, selector = _a.selector;
            return (_jsx(Avatar, { type: type, letter: letter, shape: shape, size: size, src: src, selector: selector }, letter !== null && letter !== void 0 ? letter : src));
        }) }));
}
